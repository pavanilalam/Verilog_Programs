# SR Flip-Flop

An SR Flip-Flop stores one bit of data. It has Set (`S`) and Reset (`R`) inputs and updates the output (`Q`) on the rising edge of the clock.

## Files

- `sr_flipflop.v`
- `tb_sr_flipflop.v`
- `sr_flipflop.png`
